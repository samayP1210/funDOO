export default function NotFound({msg}) {
  return (
    <div style={{ display: "flex", height: "90vh" }}>
      <div style={{ margin: "auto", fontSize: "40pt" }}>
        {msg}
      </div>
    </div>
  );
}
